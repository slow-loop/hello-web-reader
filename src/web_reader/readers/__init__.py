# Lazy or individual imports to avoid breaking on missing optional dependencies.
# Used by cli.py and runner.py to dispatch readers.
# Each reader is imported inside its respective function or in the dispatch map.
